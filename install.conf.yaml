- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.profile: profile
    ~/.aliases: aliases
    ~/.zshenv: zshenv
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
